*.iml
.gradle
/local.properties
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# IDE-locals: full IDEA folder is local config noise (was partially listed).
.idea/

# Tooling state — Claude Code session state, not project content.
.claude/

# Player-side screenshot output (script writes here, not source content).
Screens/
.tmp_screens/
